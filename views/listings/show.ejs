<% layout("/layouts/boilerplate") %>

<div class="row mt-3">
  <div class="col-8 offset-3">
    <h3>Listing Details</h3>
  </div>

  <div class="card col-6 offset-2 show-card">
    <img
      src="<%= listing.image?.url || '/images/default.jpg' %>"
      class="card-img-top show-image"
      alt="Listing image"
    />
    <div class="card-body">
      <p class="card-text">
        <b><%= listing.title %></b><br><br>
        <%= listing.description %><br><br>
        ₹ <%= listing.price ? listing.price.toLocaleString("en-IN") : "No Price/night" %><br><br>
        <%= listing.location %><br><br>
        <%= listing.country %>
      </p>
    </div>
  </div>

  <div class="col-6 offset-3 mt-3 d-flex justify-content-between">
    <!-- Edit Button -->
    <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark">Edit</a>

    <!-- Delete Form -->
    <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
      <button class="btn btn-danger">Delete</button>
    </form>
  </div>
</div>
